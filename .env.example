# Chave de acesso da API (mesma chave que está em backend/RecrutamentoIA.Api/api-credentials.json).
# Copie este arquivo para `.env.local` e preencha o valor real (o `.env.local` não vai para o git).
VITE_API_KEY=coloque-sua-chave-aqui